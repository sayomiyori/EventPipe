[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = ingest_service/tests transform_service/tests
pythonpath = .
markers =
    integration: requires external services (Kafka, PostgreSQL, MinIO, etc.)
