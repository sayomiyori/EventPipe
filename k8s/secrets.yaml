apiVersion: v1
kind: Secret
metadata:
  name: eventpipe-secrets
  namespace: eventpipe
type: Opaque
data:
  # echo -n "eventpipe" | base64
  POSTGRES_PASSWORD: ZXZlbnRwaXBl
  # echo -n "minio" | base64
  MINIO_ACCESS_KEY: bWluaW8=
  # echo -n "minio12345" | base64
  MINIO_SECRET_KEY: bWluaW8xMjM0NQ==
